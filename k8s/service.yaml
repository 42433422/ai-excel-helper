apiVersion: v1
kind: Service
metadata:
  name: xcagi-service
  labels:
    app: xcagi
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: xcagi