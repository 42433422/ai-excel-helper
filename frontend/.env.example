# XCAGI 前端（Vite）— 复制为 .env.local 后按需填写
# 后端与全栈变量清单见仓库根 ../.env.example

# 后端 API 基址（若前后端不同源）。开发环境可填 http://127.0.0.1:5000（FastAPI）。
# VITE_API_BASE 与 VITE_API_BASE_URL 等价（api/core 与 utils/apiBase 已统一读取，任选其一）。
# UPDATED 2026-04-17: 端口 8000 已删除，统一使用 5000 端口
# 切勿填 Vite 开发地址（如 http://127.0.0.1:5001），否则 /api 会请求到 Vite 自身导致流式等接口 404。
# VITE_API_BASE_URL=http://127.0.0.1:5000
# 线上 FHD / XCmax 与修茈市场隔离：主域路径前缀由 Nginx 反代到 5099（勿用根路径 https://xiu-ci.com，会与市场 /api 冲突）。
# VITE_API_BASE_URL=https://xiu-ci.com/fhd-api

# 构建产物内注入 window.__XCMAX_API_BASE__，使登录 /api/auth/login 等走 Nginx 前缀（如 /fhd-api）→ fhd-full（PostgreSQL users），与线上一致、无需改 bundle 外逻辑。
# VITE_XCMAX_PUBLIC_API_PREFIX=/fhd-api

# 不挂载一级数据库读口令全屏弹窗（开发自用；受保护 GET 仍可能 403，直至服务端关读锁或令牌正确）。
# VITE_DISABLE_GLOBAL_READ_TOKEN_PROMPT=1

# 子路径部署或网关只暴露非默认流式路径时，可指定首条 Planner SSE（须与后端真实路由一致）
# VITE_CHAT_STREAM_PATH=/api/ai/unified_chat/stream

# 与后端 FHD_DB_WRITE_TOKEN 完全一致时，聊天请求会附带 db_write_token，Planner 可执行 products_bulk_import
# 也可不在此配置，改在「系统设置」里填写并写入 localStorage xcagi_db_write_token
# VITE_DB_WRITE_TOKEN=change-me-to-match-backend

# 修茈 MODstore 工作台网页（仅外链）：XCAGI「MOD 扩展」页内「在浏览器打开工作台」按钮默认指向此地址；Catalog 数据仍走后端 /api/mod-store。
# 默认 https://xiu-ci.com/market/workbench/unified；本地开发工作台前端时可改为：
# VITE_MODSTORE_WEB_URL=http://127.0.0.1:5174

# 修茈市场网页基址；「网页查看」、模型支付页外链等默认在其下拼 /plans、/wallet。
# VITE_MARKET_BASE=https://xiu-ci.com/market
#
# --- 本机开发 + 远程市场（模型服务页余额同步）---
# 前端 Vite 代理到本机 FHD 后端（默认 5000）；市场数据由后端 .env 的 XCAGI_MARKET_BASE_URL 决定，不是 VITE_*。
# 在 FHD/.env 中设置（改后需重启 python run.py）：
#   XCAGI_MARKET_BASE_URL=https://xiu-ci.com/market
# 或直连：XCAGI_MARKET_BASE_URL=http://119.27.178.147:9999
# 冒烟：curl -sS -o /dev/null -w "%{http_code}" https://xiu-ci.com/market/api/health  期望 200
# 登录 FHD 后在浏览器 Network 查看 GET /api/market/session-handoff、POST /api/market/account-overview
# 登录页「忘记账号/忘记密码」→ 应用内 /login/forgot-*，走后端 API（本机 PG + 代理修茈市场验证码）。
# 连接生产库：在服务器或 SSH 隧道后设置 DATABASE_URL=postgresql+psycopg://...@119.27.178.147:5432/xcagi
# 市场验证码：XCAGI_MARKET_BASE_URL=http://119.27.178.147:9999（或 https://xiu-ci.com/market 反代地址）
# 「注册」→ /login/register（写入本机 PostgreSQL）。
# 非企业版默认开放自助注册；关闭：在服务器环境变量设 FHD_ALLOW_OPEN_REGISTRATION=0
# 企业版注册会同时调用修茈市场 API（需 XCAGI_MARKET_BASE_URL 可达）。
# 登录异常帮助默认应用内 /login/help（无需外网）；若已有官方帮助页可设：
# VITE_LOGIN_HELP_URL=https://example.com/your-login-help
# 「系统设置 → 修茈 MODstore 网页同步」里「打开修茈登录 / 去创建 Token / 工作台」链接均基于 VITE_MARKET_BASE。
# 可选：单独指定套餐页与钱包页（不设则分别为 VITE_MARKET_BASE/plans 与 …/wallet）
# VITE_MARKET_PLANS_URL=https://xiu-ci.com/market/plans
# VITE_MARKET_WALLET_URL=https://xiu-ci.com/market/wallet

# 离线 TTS（顶栏「下载离线包」）从 Hugging Face Hub 拉取 Xenova/mms-tts-cmn。
# 默认不在页面启动时后台预热，避免受限网络下产生 401/CORS 刷屏；如确认网络可用再开启：
# VITE_PRELOAD_OFFLINE_TTS=true
# 若出现 401/无法访问 huggingface.co，可指定镜像基址（须带尾部斜杠，与 transformers.js 的 env.remoteHost 一致）。
# 注意：镜像站必须允许浏览器跨域访问；hf-mirror.com 可能返回 200 但缺少 CORS 头，仍会被浏览器拦截。
# VITE_HF_REMOTE_HOST=https://hf-mirror.com/

# 通用宿主发行版（里程碑 I/P）：构建默认平台壳；主对话经 planner-bridge Mod 路由。也可用 frontend/.env.generic + npm run build:generic
# VITE_XCAGI_DEFAULT_PLATFORM_SHELL=1
# 临时完整 ERP：访问时加 ?full=1 ；强制壳：?shell=1

# 在线 TTS：默认使用浏览器系统语音，不主动调用后端 /api/tts。
# 如需教程步骤预取/播放后端 Edge TTS，可显式开启：
# VITE_ENABLE_TUTORIAL_ONLINE_TTS=true
