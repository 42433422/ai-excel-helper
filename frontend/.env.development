# 与同机另一套 FHD 并行时可在 .env.development.local 覆盖，例如：
# VITE_DEV_PORT=5101
# VITE_API_BASE=http://127.0.0.1:5100
#
# 开发环境：不要设置 VITE_API_BASE_URL（或保持为空）
# 这样 /api/* 使用相对路径，由 vite.config.js 代理到 http://127.0.0.1:5000，
# 与页面同源（:5001），避免跨域、以及后端冷启动/热重载时浏览器直连 5000 的 Failed to fetch / ERR_ABORTED。
#
# 仅在需要浏览器直连后端排查问题时再取消注释：
# VITE_API_BASE_URL=http://127.0.0.1:5000

# 与后端 FHD_DB_WRITE_TOKEN 一致（本地开发；生产请改密钥且勿提交真实环境文件）
VITE_DB_WRITE_TOKEN=61408693
