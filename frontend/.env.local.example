# 局域网访问配置示例
# 复制此文件为 .env.local，然后修改 VITE_API_BASE 为你的后端 IP
#
# 勿将 VITE_API_BASE 指到「仅部分接口」的第三方网关：会导致 /api/auth/session/validate、
# /api/debug/client-log 等在本仓库已实现的路由在上游 404。

# 后端地址（默认 127.0.0.1:5000；仓库内 python run.py 常用 5000，而非 8765）
# 如果后端在局域网其他机器上，改成那台机器的 IP，例如：
# VITE_API_BASE=http://192.168.1.100:5000
#
# 本地验收企业版登录页时与后端 XCAGI_PRODUCT_SKU=enterprise 对齐（仅 dev）
# VITE_XCAGI_PRODUCT_SKU=enterprise
#
# 若 Vite 终端出现 connect ETIMEDOUT / ECONNREFUSED：
# - 说明本机连不上当前 VITE_API_BASE（防火墙、安全组未放行、后端未监听 0.0.0.0、IP/端口写错）
# - 本地调试请改回 VITE_API_BASE=http://127.0.0.1:5000 并保存后重启 npm run dev
# - 远端服务器需在云平台「安全组」入站放行 TCP 端口，且进程绑定 0.0.0.0，不能只绑 127.0.0.1
VITE_API_BASE=http://127.0.0.1:5000

# 与线上一致（登录校验走服务器 PostgreSQL）：可改为完整反代根，例如
# VITE_API_BASE=https://xiu-ci.com/fhd-api
# 保存后重启 npm run dev；页面仍走同源 /api，由 Vite 代理到该地址。

# --- LAN / 401·404 排查清单（阶段 A，改环境变量后务必重启对应进程）---
# 1) FastAPI：确认在 FHD 仓库根启动（python run.py），代码更新后重启后端进程。
# 2) Vite：修改本文件或依赖后执行 npm run dev 重启；浏览器访问 http://192.168.x.x:5001 时，
#    浏览器请求同源 /api → 由 Vite 代理到上面的 VITE_API_BASE（见 vite.config.js）。
# 3) LAN_GUARD_ENABLED=1 时需后端为新版本（含 /api/mods 等前缀放行）；本地可先设 0 验证。
# 4) 登录失败且后端报数据库错误：确认 sessions 表已有 market_access_token 列（启动自动补列或 alembic）。
# 5) 一键探测后端是否就绪（阶段 B）：在仓库根 PowerShell 执行
#    .\scripts\dev\check_backend_api.ps1
#    可选参数：-BaseUrl http://127.0.0.1:5000

# 离线语音包下载走 Hugging Face；默认不启动时预热，确认网络可用后可开启
# VITE_PRELOAD_OFFLINE_TTS=true
# 受限网络可改用支持浏览器 CORS 的镜像（详见 .env.example）
# VITE_HF_REMOTE_HOST=https://hf-mirror.com/

# 在线 TTS 默认不主动调用后端 /api/tts；需要教程语音预取/播放时再开启
# VITE_ENABLE_TUTORIAL_ONLINE_TTS=true
