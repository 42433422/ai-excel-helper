# Migration Scripts

Put data/schema migration scripts here.

Requirements:

- support `--dry-run` when possible
- make side effects explicit in logs
- keep scripts idempotent where feasible

If a migration becomes part of formal release flow, move it to the main migration system.
